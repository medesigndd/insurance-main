export interface gender {
  id: number;
  code: string;
  view: string;
}

export interface insurancePackage {
  id: number;
  code: string;
  view: string;
}

export interface paymentFrequency {
  id: number;
  code: string;
  view: string;
}